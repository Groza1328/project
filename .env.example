# Переменные для Railway (Web-сервис). PostgreSQL — отдельный сервис в Railway.
SPRING_PROFILES_ACTIVE=prod

# Почта
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your@gmail.com
MAIL_PASSWORD=app_password_here
MAIL_FROM=your@gmail.com

# Локально (профиль local) — не нужны на Railway, если PostgreSQL подключён как plugin
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=sibmobile
# DB_USER=postgres
# DB_PASSWORD=
